# Copilot Instructions

## General Guidelines
- When renaming a parameter to avoid shadowing (e.g., to 'selectedProfileArg'), use that name consistently throughout the method to avoid referencing an undeclared local named 'selectedProfile'.

## Code Style
- Use specific formatting rules
- Follow naming conventions